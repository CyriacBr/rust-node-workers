{
  "name": "rust-node-workers",
  "version": "0.0.0",
  "main": "dist/bridge.js",
  "types": "dist/bridge.d.ts",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.23",
    "ts-lib": "^0.0.5",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "build": "cargo build --release && tsc"
  }
}
